import 'package:flutter/material.dart';
class BrowsenetScreen extends StatelessWidget {
  const BrowsenetScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
